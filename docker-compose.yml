version: '3.8'

services:
  backend:
    build:
      context: ./server
      dockerfile: Dockerfile
    container_name: lifeos_backend
    restart: unless-stopped
    environment:
      - PORT=5000
      - MONGO_URI=${MONGO_URI}
      - JWT_SECRET=${JWT_SECRET}
      # Add other SMTP or backend env vars here if needed
    ports:
      - "5000:5000"

  frontend:
    build:
      context: ./client
      dockerfile: Dockerfile
      args:
        # We set VITE_API_URL to /api because Nginx will internally proxy /api traffic to the backend container
        - VITE_API_URL=/api
    container_name: lifeos_frontend
    restart: unless-stopped
    ports:
      # Map host port 80 to container port 80 (change the host port on the left if you run multiple apps)
      - "80:80"
    depends_on:
      - backend
